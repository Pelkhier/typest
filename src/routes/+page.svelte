<script>
    import { Spinner } from "$lib/components";

    // TODO : in general, this application needs dynamic error pages, such as "not found" error page and so on...
    // TODO : this project has types definitions scattered around the files, it will be better to have a single file to have them all in
</script>

<Spinner
    wrapperClass="bg-gostwhite dark:bg-darkblue opacity-80 fixed z-[999] top-0 right-0 left-0 bottom-0"
    loaderClass="text-tomato w-[1.5em] h-[1.5em] text-lg"
/>
